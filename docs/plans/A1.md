<!--
  Copyright (c) 2020-2026 gridDigIt Kft.
  Licensed under the EUPL-1.2-or-later.
  SPDX-License-Identifier: EUPL-1.2+
-->

# A1 — Claude Code workspace setup

| Field | Value |
| --- | --- |
| Status | In review ([PR #39](https://github.com/griddigit-ci/CimPal/pull/39)) |
| Phase | 0 |
| Depends on | — |
| Size | S |
| Branch | `feature/a1-claude-workspace` |

## Goal

Make the repo ready for plan-driven work: Claude Desktop plans, Claude Code implements, `docs/plans/` is the handoff.

## Scope

- `.claude/settings.json` (committed): permissions and an end-of-turn test hook
- `.claude/rules/security.md` and `.claude/rules/testing.md` (path-scoped)
- `.claude/skills/`: `/add-regression-test`, `/security-check-change`, `/end-session`, `/wp-footer`
- `.claude/agents/`: `security-reviewer` (read-only), `test-writer`
- Short "Current work" pointer in `CLAUDE.md`
- `.gitignore`: stop ignoring all of `/.claude/`; ignore only `settings.local.json` and `CLAUDE.local.md`

## Acceptance criteria (status checklist)

- [x] `claude` starts in the repo and loads the settings, rules, skills and agents without errors
- [x] `git push`, `gh release` and `scripts/New-ReleaseTag.ps1` are denied by settings (`git push` changed to *ask*, per the maintainer's decision; tag and force pushes stay denied)
- [x] The test hook runs only the tests of the module whose Java files changed
- [x] `CLAUDE.md` stays under ~150 lines (99)

## Instructions for Claude Code

Start a session with: `Execute docs/plans/A1.md` (in plan mode).

```text
Set up this repo for plan-driven work between Claude Desktop (planning) and Claude Code (implementation). docs/plans/ already exists with one file per work package; do not rewrite those files except to tick A1's checklist.
1. First check your own current documentation for: CLAUDE.md @imports, .claude/rules (path-scoped), .claude/settings.json permissions and hooks, .claude/skills, .claude/agents. Use only features that exist in your installed version.
2. Add a short "Current work" section to CLAUDE.md pointing to docs/plans/README.md (keep CLAUDE.md under ~150 lines; move detail into imported files).
3. .claude/settings.json (committed): allow mvn and git status/diff/log/branch/checkout/add/commit; deny git push, gh release, scripts/New-ReleaseTag.ps1, and reading *.token, .env and credential files. Add a hook that runs `mvn -B -q -pl <module> -am test` for the module whose Java files changed, at the end of a turn (not after every single edit).
4. .claude/rules/security.md scoped to ServeCommand, McpCommand, RunCommand, ValidationTools, ExcelTools, archive/zip code and the Main ai package: never weaken the egress policy, fail closed on failed imports, every change needs a regression test.
   .claude/rules/testing.md scoped to **/src/test/**: no network, @TempDir, fixtures under src/test/resources/fixtures/<feature>/, snapshot updates only via -Dsnapshot.update=true.
5. Skills: /add-regression-test, /security-check-change (checklist from SECURITY-SELF-ATTESTATION.md §5.3 item 5), /end-session (tick the plan checklist + update PROJECT.md), /wp-footer (the standard footer in docs/plans/README.md).
6. Agents: security-reviewer (read-only tools; reviews a diff against the security rules), test-writer (writes tests for a named class using the fixture rules).
7. .gitignore: replace the blanket /.claude/ ignore so settings.json, rules, skills and agents are committed; ignore .claude/settings.local.json and CLAUDE.local.md.
```

Standard footer (applies to every work package):

- Read CLAUDE.md, docs/PROJECT.md and this plan file first. Do not redo full codebase discovery.
- Start in plan mode: propose the plan, list the files you will touch, wait for approval.
- Work on branch feature/<wp-id>-<short-name> off devel. Do not push, tag or open a PR unless asked.
- Follow CLAUDE.md conventions (license header, package case, JPMS requires, logic in Core).
- Tests first where possible. Finish with `mvn -B verify` green and report the test count before and after.
- Run /security-review on the diff if the change touches I/O, network, processes, parsing or serve/mcp/run.
- At the end: tick this file's checklist, add to its decisions log, update docs/PROJECT.md (date, status, known issues, next steps) and docs/cli/* if CLI behaviour changed. Summarise what changed and what is left open.

## Decisions log

| Date | Decision | By |
| --- | --- | --- |
| 2026-09-25 | `git push` goes under `ask`, not `deny`: Claude can push only after explicit approval. `gh release`, the release script, and tag and force pushes stay denied. | Maintainer |
| 2026-09-25 | `.claude/rules/security.md` scope extended beyond the plan with `PythonShaclValidator` (process execution) and `ComparisonCsvWriter` (CSV emission), both §5.3 item 5 categories. Archive scope = classes using `java.util.zip` (`ModelFactory` ×2, `InstanceDataFactory` ×2, `WizardContext`, `RDFVisualisationController`). | Claude Code |
| 2026-09-25 | The Stop hook detects changes against `HEAD` (tracked and untracked `*.java`) and fingerprints them in `.claude/state/last-green`, so turns without new Java edits don't rerun Maven. It runs under Git Bash; `.gitattributes` keeps `*.sh` on LF because the repo uses `core.autocrlf=true`. | Claude Code |
| 2026-09-25 | `.gitignore` now tracks all of `.claude/` except `settings.local.json`, `worktrees/` and `state/`, and ignores `CLAUDE.local.md`. | Claude Code |
| 2026-09-25 | `/end-session` and `/wp-footer` are user-invoked only (`disable-model-invocation: true`). CLAUDE.md stays a single file (99 lines), so `@` imports aren't needed yet. | Claude Code |

## Notes and results

- **Features used** (checked against the Claude Code v2.1.282 docs): `.claude/rules/*.md` with `paths:` frontmatter, `Bash(...)`/`PowerShell(...)`/`Read(...)` permission rules, the `Stop` hook (exit 2 feeds the failure back to Claude, and `stop_hook_active` guards against loops), `.claude/skills/<name>/SKILL.md`, and `.claude/agents/<name>.md` with a `tools:` allowlist.
- **Tests:** baseline 73 (Core 65, Main 8, CLI 0) and 73 after. This is a config-only change.
- **`mvn -B verify` is not green on this machine, for an environmental reason.** CimPal-CLI packaging fails with "Could not create modular JAR file" because two `CimPal-CLI.jar mcp` processes (Claude Desktop's MCP server) lock the JAR. All tests pass with `mvn -B test`. The failure happens with or without this change. It's recorded in CLAUDE.md gotchas and in PROJECT.md known issues.
- **Hook verification** (simulated stdin):
  - No Java changes: exits 0 in 0.2 s.
  - A deliberately failing Core test: exits 2 with the failure on stderr, and ran only `-pl CimPal-Core -am`.
  - After the fix: exits 0 and writes the fingerprint.
  - A rerun: skips in 0.5 s.
  - `stop_hook_active: true`: exits 0.
- **Headless `claude -p` in the repo:**
  - Agents `security-reviewer` and `test-writer` loaded, as did the model-invokable skills.
  - `gh release list` and `New-ReleaseTag.ps1` were denied, and `git push --dry-run` required approval.
  - Reading `serve.token` was denied.
- **Open item for each developer:** open `claude` interactively in the repo once and accept the workspace-trust dialog. Until then Claude Code ignores the project `allow` rules. Deny and ask rules still apply. Then check `/hooks`, `/agents` and `/context`.
- **Known limitation:** `Read(...)` deny rules don't stop shell commands such as `cat` or `Get-Content` from reading those files. They're a guardrail, not a sandbox.
- `/security-review` wasn't run, because no application code changed.
